version: '3.8'
services:
  localstack:
    image: localstack/localstack:latest
    ports:
      - "4566:4566"
      - "4571:4571"
    environment:
      - SERVICES=s3,lambda
      - DEBUG=1
    volumes:
      - "localstack_data:/var/lib/localstack"

volumes:
  localstack_data:
